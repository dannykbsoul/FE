## 1.常见排序算法总结

## 2.数组相关算法题总结

### 1.两数之和(1)

```
给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出 和为目标值 的那 两个 整数，并返回它们的数组下标。
输入：nums = [2,7,11,15], target = 9
输出：[0,1]
解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。
```

> 首先考虑先排序然后双指针，但是因为该题目要求返回原数组的下标，所以不能排序
>
> 双重循环也能做到，但是时间复杂度 O(n^2)
>
> 可以考虑用空间复杂度优化时间复杂度，用 O(n)的空间复杂度使得时间复杂度也为 O(n)

```JS
var twoSum = function(nums, target) {
    const map=new Map();
    for(let i=0,len=nums.length;i<len;i++) {
        let findNum=target-nums[i];
        if(map.has(findNum)) return [map.get(findNum),i];
        else map.set(nums[i],i);
    }
};
```

### 2.三数之和(15)

```
给你一个包含 n 个整数的数组 nums，判断 nums 中是否存在三个元素 a，b，c ，使得 a + b + c = 0 ？请你找出所有和为 0 且不重复的三元组。
输入：nums = [-1,0,1,2,-1,-4]
输出：[[-1,-1,2],[-1,0,1]]
```

> 暴力解法，三重循环，时间复杂度 O(n^3)
>
> 因为这题返回的并不是原数组的下标，所以考虑先排序，其中 sort 函数默认是按照字典序进行，即按字母顺序对数组中的元素进行排序，想要对数字进行排列，需要传入比较函数
>
> 接着进行遍历，然后在剩下的序列中进行双指针
> ⚠️ 因为要求三元组不能重复，所以需要加一些条件进行去重

```JS
var threeSum = function(nums) {
    const res=[];
    nums.sort((a,b)=>a-b);
    for(let i=0,len=nums.length;i<len-2;i++) {
        // 去重
        if(i&&nums[i]===nums[i-1]) continue;
        let target = -nums[i],
            l=i+1,
            r=len-1;
        while(l<r) {
            let sum=nums[l]+nums[r];
            if(sum===target) {
                res.push([nums[i],nums[l++],nums[r--]]);
                // 去重
                while(nums[l]===nums[l-1]) l++;
                while(nums[r]===nums[r+1]) r--;
            }
            else if(sum<target) l++;
            else r--;
        }
    }
    return res;
};
```

### 3.最接近的三数之和(16)

```
给定一个包括 n 个整数的数组 nums 和 一个目标值 target。找出 nums 中的三个整数，使得它们的和与 target 最接近。返回这三个数的和。假定每组输入只存在唯一答案。

输入：nums = [-1,2,1,-4], target = 1
输出：2
解释：与 target 最接近的和是 2 (-1 + 2 + 1 = 2) 。
```

> 和上题的三数之和类似

```JS
var threeSumClosest = function(nums, target) {
    let minDis=Infinity,
        res;
    nums.sort((a,b)=>a-b);
    for(let i=0,len=nums.length;i<len-2;i++) {
        if(i&&nums[i]===nums[i-1]) continue;
        let l=i+1,
            r=len-1;
        while(l<r) {
            let sum=nums[i]+nums[l]+nums[r];
            if(sum===target) return target;
            else if(sum<target) {
                if(Math.abs(sum-target)<minDis) {
                    minDis=Math.abs(sum-target);
                    res=sum;
                }
                l++;
            }else {
                if(Math.abs(sum-target)<minDis) {
                    minDis=Math.abs(sum-target);
                    res=sum;
                }
                r--;
            }
        }
    }
    return res;
};
```

### 4.四数之和(18)

```
给定一个包含 n 个整数的数组 nums 和一个目标值 target，判断 nums 中是否存在四个元素 a，b，c 和 d ，使得 a + b + c + d 的值与 target 相等？找出所有满足条件且不重复的四元组。

给定数组 nums = [1, 0, -1, 0, -2, 2]，和 target = 0。

满足要求的四元组集合为：
[
  [-1,  0, 0, 1],
  [-2, -1, 1, 2],
  [-2,  0, 0, 2]
]
```

> if(i&&nums[i]===nums[i-1]) continue; 确保 nums[0]改变了
>
> if(j>i+1&&nums[j]===nums[j-1]) continue; 确保 nums[1]改变了
>
> while(nums[l]===nums[l-1]) l++; 确保 nums[2]改变了
>
> while(nums[r]===nums[r+1]) r--; 确保 nums[3]改变了

```JS
var fourSum = function(nums, target) {
    nums.sort((a,b)=>a-b);
    const res=[];
    for(let i=0,len=nums.length;i<len-3;i++) {
        if(i&&nums[i]===nums[i-1]) continue;
        for(let j=i+1;j<len-2;j++) {
            if(j>i+1&&nums[j]===nums[j-1]) continue;
            let l=j+1,
                r=len-1;
            while(l<r) {
                let sum=nums[i]+nums[j]+nums[l]+nums[r];
                if(sum===target) {
                    res.push([nums[i],nums[j],nums[l++],nums[r--]]);
                    while(nums[l]===nums[l-1]) l++;
                    while(nums[r]===nums[r+1]) r--;
                }else if(sum<target) l++;
                else r--;
            }
        }
    }
    return res;
};
```

### 5.长度最小的连续子数组(209)

```
给定一个含有 n 个正整数的数组和一个正整数 s ，找出该数组中满足其和 ≥ s 的长度最小的 连续 子数组，并返回其长度。如果不存在符合条件的子数组，返回 0。

输入：s = 7, nums = [2,3,1,2,4,3]
输出：2
解释：子数组 [4,3] 是该条件下的长度最小的子数组。
```

> 暴力解法 O(n^2)
>
> 滑动窗口
>
> 我们把数组中的元素不停的入队，直到总和大于等于 s 为止，接着记录下队列中元素的个数，然后再不停的出队，直到队列中元素的和小于 s 为止（如果不小于 s，也要记录下队列中元素的个数，这个个数其实就是不小于 s 的连续子数组长度，我们要记录最小的即可）。接着再把数组中的元素添加到队列中……重复上面的操作，直到数组中的元素全部使用完为止。
>

```JS
var minSubArrayLen = function(s, nums)  {
    let min=Infinity,
        len=nums.length,
        sum=0,
        l=0,
        r=0;
    while(r<len) {
        sum+=nums[r++]
        while(sum>=s) {
            min=Math.min(min,r-l);
            sum-=nums[l++];
        }
    }
    return min===Infinity?0:min;
};
```

> 二分查找
>
> 二分数组的长度，然后以该长度判断是否满足条件，满足的话继续缩小数组长度，否则扩大数组长度
>
> if(i>=size) sum-=nums[i-size];
>
> 用于保证此时sum的和是 <=size个 数的和

~~~JS
var minSubArrayLen = function(s, nums)  {
    let min=0,
        len=nums.length,
        l=1,
        r=len;
    function helper(size) {
        let sum=0;
        for(let i=0;i<len;i++) {
            sum+=nums[i];
            if(i>=size) sum-=nums[i-size];
            if(sum>=s) return true;
        }
    }
    while(l<=r) {
        let mid=Math.floor(l+(r-l)/2);
        if(helper(mid)) {
            min=mid;
            r=mid-1;
        }else l=mid+1;
    }
    return min;
};
~~~

### 6.无重复字符的最长子串(3)

~~~
给定一个字符串，请你找出其中不含有重复字符的 最长子串 的长度。
输入: s = "abcabcbb"
输出: 3 
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。
~~~

> 类似于滑动窗口的改进版
>
> 用map来存放元素出现的下标，当碰到出现过的元素，那么此时窗口的left置为上次出现元素的下标+1，窗口的right每一次迭代都会去计算新的最长子串。

~~~JS
var lengthOfLongestSubstring = function(s) {
    let maxLen=0,
        l=0,
        r=0,
        len=s.length,
        map=new Map();
    while(r<len) {
        let c=s[r];
        if(map.has(c)&&map.get(c)>=l) {
            l=map.get(c)+1;
        }
        maxLen=Math.max(maxLen,r-l+1);
        map.set(c,r++);
    }
    return maxLen;
};
~~~

### 7.最大子序和(53)

~~~
给定一个整数数组 nums ，找到一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。
输入: [-2,1,-3,4,-1,2,1,-5,4]
输出: 6
解释: 连续子数组 [4,-1,2,1] 的和最大，为 6。
~~~

> dp[i]：以nums[i]为结尾的最大子序和
>
> dp[i]=Math.max(dp[i-1]+nums[i],nums[i]);

~~~JS
var maxSubArray = function(nums) {
    let len=nums.length,
        dp=Array(len);
    if(!len) return 0;
    dp[0]=nums[0];
    for(let i=1;i<len;i++) {
        dp[i]=Math.max(dp[i-1]+nums[i],nums[i]);
    }
    return Math.max(...dp);
};
~~~

~~~JS
var maxSubArray = function(nums) {
    let res=nums[0],
        sum=0;
    for(let v of nums) {
        if(sum>0) sum+=v;
        else sum=v;
        res=Math.max(res,sum);
    }
    return res;
};
~~~

### 

### 8.最长公共前缀(14)

~~~
编写一个函数来查找字符串数组中的最长公共前缀。
如果不存在公共前缀，返回空字符串 ""。

输入：strs = ["flower","flow","flight"]
输出："fl"
~~~

> 取数组第一项，从长到短遍历

~~~JS
var longestCommonPrefix = function(strs) {
    if(!strs.length) return "";
    let len=strs.length;
    for(let i=strs[0].length-1;i>=0;i--) {
        let str=strs[0].slice(0,i+1),
            index=1;
        while(index<len) {
            if(strs[index].indexOf(str)!==0) break;
            index++;
        }
        if(index===len) return str;
    }
    return "";
};
~~~

### 9.删除排序数组中的重复项(26)

~~~
给定一个排序数组，你需要在 原地 删除重复出现的元素，使得每个元素只出现一次，返回移除后数组的新长度。
不要使用额外的数组空间，你必须在 原地 修改输入数组 并在使用 O(1) 额外空间的条件下完成。


给定数组 nums = [1,1,2], 
函数应该返回新的长度 2, 并且原数组 nums 的前两个元素被修改为 1, 2。 
你不需要考虑数组中超出新长度后面的元素。
~~~

> cur是待放入的空位，此时去遍历数组，找到和前一个不同的就移到cur位置上，并cur+1

~~~JS
var removeDuplicates = function(nums) {
    let cur=1,
        len=nums.length;
    for(let i=1;i<len;i++) {
        if(nums[i]!==nums[cur-1]) nums[cur++]=nums[i];
    }
    return cur;
};
~~~



### 10.合并区间(56)

~~~
给出一个区间的集合，请合并所有重叠的区间。

输入: intervals = [[1,3],[2,6],[8,10],[15,18]]
输出: [[1,6],[8,10],[15,18]]
解释: 区间 [1,3] 和 [2,6] 重叠, 将它们合并为 [1,6].
~~~

> lastIndex是需要放入的最新的元素，和上面一题类似，需要合并的合并，不需要的就覆盖

~~~JS
var merge = function(intervals) {
    intervals.sort((a,b)=>a[0]-b[0]);
    for(let i=1,len=intervals.length;i<len;i++) {
        let prev=intervals[i-1],
            cur=intervals[i];
        if(prev[1]>=cur[0]) {
            intervals[i]=[prev[0],Math.max(prev[1],cur[1])];
            intervals[i-1]=false;
        }
    }
    return intervals.filter(x=>x);
};

var merge = function(intervals) {
    intervals.sort((a,b)=>a[0]-b[0]);
    let lastIndex=1;
    for(let i=1,len=intervals.length;i<len;i++) {
        let prev=intervals[lastIndex-1],
            cur=intervals[i];
        if(prev[1]>=cur[0]) {
            intervals[lastIndex-1]=[prev[0],Math.max(prev[1],cur[1])];
        }else intervals[lastIndex++]=intervals[i];
    }
    return intervals.slice(0,lastIndex);
};
~~~



## 3.栈、队列相关算法题总结

## 4.链表相关算法题总结

## 5.树相关算法题总结

## 6.递归、回溯法相关算法题总结

## 7.动态规划相关算法题总结
